name: token-optimizer
version: "5.10.0"
description: "Token Optimizer for Hermes — proactive context-fill nudges before each turn, per-turn token accumulation, session rollup into the shared dashboard, and /token-optimizer slash command."
author: AlexGreenshpun
hooks:
  - pre_llm_call
  - post_api_request
  - on_session_finalize
  - on_session_end
